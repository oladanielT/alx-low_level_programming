#include "main.h"
/**
 * _abs - function to print the absolute value of an integer
 * @n: integer to print
 * Return: returning n
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}

	return (-1);
}
