#include "main.h"
void _puts(char *str);
#include <stdio.h>

/**
 * main - funtion to print using puts
 * Return: Always 0
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
