#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: first arguement to mul
 * @b: second arguement to mul
 * Return: Always 0
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
